# to get the current libraries installed in the current env
- **pip freeze**

# to transfer the libraries in a text file
- **pip freeze > file.txt**

# to install from requirement.txt file
- **pip install -r file.txt**